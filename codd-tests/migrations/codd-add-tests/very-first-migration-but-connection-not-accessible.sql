-- Anything will do. The test won't have created a database and this migration
-- is with the default connection string, so it will fail to even connect, and
-- should then print a nice error message.
SELECT 1;
